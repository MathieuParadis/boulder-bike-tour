// CONFIG IMPORTS
import React, {useEffect} from 'react';

const Community = () => {
  useEffect(() => {
    window.scrollTo(0, 0);
  }, []);

  return (
    <div className="community">
      community
    </div>
  );
};

export default Community;